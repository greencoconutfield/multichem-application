define(
    ['app'],
    function(app) {
        'use strict';

        var mainController = function($scope) {
        };

        app.getNgModule().controller('MainController', ['$scope', mainController]);
    }
);



