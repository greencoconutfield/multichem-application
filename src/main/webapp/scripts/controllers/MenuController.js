define(
    ['app'],
    function(app) {
        'use strict';

        var menuCtrl = function ($scope) {
        };

        app.getNgModule().controller('MenuController', ['$scope', menuCtrl]);
    }
);
